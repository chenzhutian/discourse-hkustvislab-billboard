import { withPluginApi } from 'discourse/lib/plugin-api';

var inject = Ember.inject;

export default {
  name: 'start-billboard',

  initialize(container) {
    console.debug('start-billboard');
  },
};